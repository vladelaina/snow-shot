export enum CaptureStep {
    Select = 'select',
    Draw = 'draw',
}

export enum DrawState {
    Idle = 'idle',
    Resize = 'resize',
    Select = 'select',
    Pen = 'pen',
    Rect = 'rect',
    Ellipse = 'ellipse',
    Mosaic = 'mosaic',
    Highlight = 'highlight',
    Text = 'text',
}
